# MongoBranch Configuration
# Override with env var: MONGOBRANCH_URI
#
# Default: docker compose up -d → localhost:27017 (MongoDB default port)

uri: "mongodb://localhost:27017/?directConnection=true"
sourceDatabase: "ecommerce_app"
metaDatabase: "__mongobranch"
branchPrefix: "__mb_"
